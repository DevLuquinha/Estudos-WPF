<UserControl x:Class="Learning_WPF.View.UserControls.ClearableTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Learning_WPF.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="250">
    <Grid Background="White">
        
        <TextBox x:Name="inputTextBox"  VerticalAlignment="Center"
                 FontSize="16" FontWeight="Light" Background="Transparent"
                 TextChanged="inputTextBox_TextChanged"/>
        
        <TextBlock x:Name="placeHolderTextBlock" Text="{Binding Placeholder}" 
                   FontSize="16" FontWeight="Light" Foreground="Gray"
                   VerticalAlignment="Center" Margin="5,0,0,0"
                   Panel.ZIndex="-1"/>
        
        <Button x:Name="clearButton" Width="30" HorizontalAlignment="Right"
                Content="X" Background="Transparent" Foreground="LightPink" 
                BorderThickness="0"
                Click="clearButton_Click"/>
    </Grid>
</UserControl>
